name: Build Packages

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  build-deb:
    name: Build DEB packages
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      version: ${{ steps.get-version.outputs.version }}

    strategy:
      matrix:
        target:
          - jammy
          - bookworm
        platform:
          - linux/amd64

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version from DALEC config
        id: get-version
        run: |
          VERSION=$(grep '^version:' dalec-spry.yaml | awk '{print $2}')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Download import_map.json and deno.jsonc
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          curl -H "Authorization: Bearer $GH_TOKEN" \
            -o import_map.json \
            https://raw.githubusercontent.com/programmablemd/spry/refs/heads/main/import_map.json
          curl -H "Authorization: Bearer $GH_TOKEN" \
            -o deno.jsonc \
            https://raw.githubusercontent.com/programmablemd/spry/refs/heads/main/deno.jsonc

      - name: Compile spry binary
        env:
          DENO_AUTH_TOKENS: ${{ secrets.DENO_AUTH_TOKENS }}
        run: |
          deno compile \
            --allow-all \
            --include web-ui \
            --import-map=import_map.json \
            --output=spry \
            spry.ts

          echo "✅ spry binary compiled."

      - name: Run binary tests
        run: |
          chmod +x scripts/test-binary.sh
          scripts/test-binary.sh ./spry
          echo "✅ All binary tests passed!"

      - name: Prepare binary for DALEC
        run: |
          mkdir -p src/src/man
          cp spry src/src/spry
          cp man/spry.1 src/src/man/spry.1
          chmod +x src/src/spry

          echo "✅ Binary prepared in src/src/ directory."

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker-container
          driver-opts: |
            network=host

      - name: Build DEB package for ${{ matrix.target }}
        run: |
          docker buildx build \
            --target ${{ matrix.target }} \
            --output type=local,dest=./output/${{ matrix.target }} \
            -f dalec-spry.yaml \
            .

      - name: List output files
        run: |
          echo "Output directory contents:"
          find ./output -type f -name "*.deb" || echo "No .deb files found"
          ls -lah ./output/${{ matrix.target }}/ || echo "Directory not found"

      - name: Upload DEB artifact
        uses: actions/upload-artifact@v4
        with:
          name: spry-${{ matrix.target }}-deb
          path: ./output/${{ matrix.target }}/*.deb



  build-windows:
    name: Build Windows package
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Download import_map.json and deno.jsonc
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          curl -H "Authorization: Bearer $GH_TOKEN" \
            -o import_map.json \
            https://raw.githubusercontent.com/programmablemd/spry/refs/heads/main/import_map.json
          curl -H "Authorization: Bearer $GH_TOKEN" \
            -o deno.jsonc \
            https://raw.githubusercontent.com/programmablemd/spry/refs/heads/main/deno.jsonc

      - name: Compile spry binary for Windows
        env:
          DENO_AUTH_TOKENS: ${{ secrets.DENO_AUTH_TOKENS }}
        run: |
          deno compile \
            --allow-all \
            --include web-ui \
            --target x86_64-pc-windows-msvc \
            --import-map=import_map.json \
            --output=spry.exe \
            spry.ts

          echo "✅ spry.exe binary compiled for Windows."

      - name: Verify Windows binary
        run: |
          if [ -f "spry.exe" ]; then
            echo "✅ Windows binary created successfully"
            ls -lh spry.exe
            file spry.exe
          else
            echo "❌ Windows binary not found"
            exit 1
          fi

      - name: Package Windows binary
        run: |
          mkdir -p output
          zip output/spry-windows.zip spry.exe

      - name: Upload Windows spry artifact
        uses: actions/upload-artifact@v4
        with:
          name: spry-windows
          path: output/spry-windows.zip

  build-macos:
    name: Build macOS binaries
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Download import_map.json and deno.jsonc
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          curl -H "Authorization: Bearer $GH_TOKEN" \
            -o import_map.json \
            https://raw.githubusercontent.com/programmablemd/spry/refs/heads/main/import_map.json
          curl -H "Authorization: Bearer $GH_TOKEN" \
            -o deno.jsonc \
            https://raw.githubusercontent.com/programmablemd/spry/refs/heads/main/deno.jsonc

      - name: Compile spry for macOS
        env:
          DENO_AUTH_TOKENS: ${{ secrets.DENO_AUTH_TOKENS }}
        run: |
          deno compile \
            --allow-all \
            --include web-ui \
            --import-map=import_map.json \
            --output=spry-macos \
            spry.ts

          echo "✅ spry macOS binary compiled."

      - name: Run binary tests
        run: |
          chmod +x scripts/test-binary.sh
          scripts/test-binary.sh ./spry-macos
          echo "✅ All macOS binary tests passed!"

      - name: Create macOS archive
        run: |
          mkdir -p output
          tar -czf output/spry-macos.tar.gz spry-macos

          echo "📦 macOS archive created:"
          ls -lh output/

      - name: Upload macOS spry artifact
        uses: actions/upload-artifact@v4
        with:
          name: spry-macos
          path: ./output/spry-macos.tar.gz

  release:
    name: Create Release
    needs: [build-deb, build-windows, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded artifacts
        run: |
          echo "📦 Downloaded artifacts:"
          find ./artifacts -type f -name "*.deb" -o -name "*.tar.gz" -o -name "*.zip" | head -20
          ls -la ./artifacts/ || echo "No artifacts directory"

      - name: Organize release files
        run: |
          mkdir -p release
          # Copy DEB packages
          find artifacts -name "*.deb" -exec cp {} release/ \;
          # Copy macOS archives
          find artifacts -name "*.tar.gz" -exec cp {} release/ \;
          # Copy Windows archive
          find artifacts -name "*.zip" -exec cp {} release/ \;

          # Create generic names for "latest" links as requested
          # For Ubuntu 22.04 (Jammy)
          cp release/spry_*ubuntu22.04u1_amd64.deb release/spry-ubuntu22.04u1_amd64.deb || true
          # For Debian 12 (Bookworm)
          cp release/spry_*debian12u1_amd64.deb release/spry-debian12u1_amd64.deb || true
          
          # macOS and Windows already have generic names, but let's ensure we have them ready
          # spry-macos.tar.gz and spry-windows.zip are already in release/

          # List all files for verification
          echo "📦 Release files:"
          ls -R release/

      - name: Update latest tag
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git tag -f latest ${{ github.sha }}
          git push -f origin latest

      - name: Update Latest Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: latest
          files: |
            release/spry-ubuntu22.04u1_amd64.deb
            release/spry-debian12u1_amd64.deb
            release/spry-macos.tar.gz
            release/spry-windows.zip
          draft: false
          prerelease: false
          name: "Latest Release"
          body: |
            This release always contains the latest stable version of Spry.
            Current version: ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          draft: false
          prerelease: false
          body: |
            ## Spry ${{ github.ref_name }}

            ### � Latest Stable Version (Recommended)
            You can always download the latest version using these stable links:

            #### Ubuntu 22.04 (Jammy)
            ```bash
            wget https://github.com/${{ github.repository }}/releases/download/latest/spry-ubuntu22.04u1_amd64.deb
            sudo dpkg -i spry-ubuntu22.04u1_amd64.deb
            ```

            #### Debian 12 (Bookworm)
            ```bash
            wget https://github.com/${{ github.repository }}/releases/download/latest/spry-debian12u1_amd64.deb
            sudo dpkg -i spry-debian12u1_amd64.deb
            ```

            #### macOS
            ```bash
            wget https://github.com/${{ github.repository }}/releases/download/latest/spry-macos.tar.gz
            tar -xzf spry-macos.tar.gz
            sudo cp spry-macos /usr/local/bin/spry
            chmod +x /usr/local/bin/spry
            ```

            #### Windows
            Download [spry-windows.zip](https://github.com/${{ github.repository }}/releases/download/latest/spry-windows.zip) and extract it.

            ---

            ### 📦 Versioned Packages (${{ github.ref_name }})

            #### Ubuntu 22.04 (Jammy)
            ```bash
            wget https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/spry_${{ needs.build-deb.outputs.version }}-ubuntu22.04u1_amd64.deb
            sudo dpkg -i spry_${{ needs.build-deb.outputs.version }}-ubuntu22.04u1_amd64.deb
            ```

            #### Debian 12 (Bookworm)
            ```bash
            wget https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/spry_${{ needs.build-deb.outputs.version }}-debian12u1_amd64.deb
            sudo dpkg -i spry_${{ needs.build-deb.outputs.version }}-debian12u1_amd64.deb
            ```

            #### macOS
            ```bash
            wget https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/spry-macos.tar.gz
            ```

            #### Windows
            ```powershell
            # Download spry-windows.zip from this release
            ```

            ### 🔍 Verification
            After installation, verify that spry is working correctly:
            ```bash
            spry --version
            spry --help
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
